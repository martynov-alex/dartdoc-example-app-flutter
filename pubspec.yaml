name: dartdoc_example_flutter
description: Article and an example app about using dartdoc.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.6
  dio: ^5.3.3
  elementary: ^3.0.0
  elementary_helper: ^1.0.1
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  provider: ^6.0.5
  retrofit: ^4.0.3

dev_dependencies:
  build_runner: ^2.4.6
  flutter_test:
    sdk: flutter
  freezed: ^2.4.5
  json_serializable: ^6.7.1
  retrofit_generator: ^8.0.2
  surf_lint_rules: ^3.0.0

flutter:
  uses-material-design: true
